#include <stdio.h>
int main(){

    int students_mark[5];
    for (int i = 0; i < 5; i++){
        scanf("%d", &students_mark[i]);

        printf("%d\n", students_mark[i]);
    }
        return 0;
} 